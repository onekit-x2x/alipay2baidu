<image 
class="onekit-image {{Class}}"
style="{{Style}}"
mode="{{mode}}" 
src="{{src}}"
lazy-load="{{lazyLoad}}"
default-source="{{defaultSource}}"

bindload="image_load"
onError="image_error"
onTap="image_tap"

>
   <slot/>
</image>