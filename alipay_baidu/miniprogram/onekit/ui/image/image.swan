<image 
class="onekit-image {{onekitClass}}"
style="{{onekitStyle}}" id="{{onekitId}}"
mode="{{mode}}" 
src="{{src}}"
lazy-load="{{lazyLoad}}"
default-source="{{defaultSource}}"

bindload="image_load"
binderror="image_error"
catchtap="image_tap"

>

</image>