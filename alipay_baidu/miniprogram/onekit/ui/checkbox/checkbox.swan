<view>
<checkbox 
class="onekit-checkbox {{onekitClass}}"
style="{{onekitStyle}}" id="{{Id}}"
  value="{{value}}" 
  disabled="{{disabled}}"
  checked="{{checked}}"
  color="{{color}}"
  onChange="{{onChange}}}"/>
<slot/>
</view>