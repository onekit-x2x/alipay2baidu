<view>
<checkbox 
class="onekit-checkbox {{Class}}"
style="{{Style}}" id="{{Id}}"
  value="{{value}}" 
  disabled="{{disabled}}"
  checked="{{checked}}"
  color="{{color}}"
  onChange="{{onChange}}}"/>
<slot/>
</view>